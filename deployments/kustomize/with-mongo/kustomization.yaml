apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../install

components:
  - ../components/mongodb

patches:
  - path: patches/api-mongodb.yaml
    target:
      group: apps
      version: v1
      kind: Deployment
      name: sarcoma-fasttrack-api
